<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chs.dao.ZoneMapper">

    <select id="getFollowee" resultType="java.lang.Integer">
        select followee from following where follower=#{user_Id}
    </select>
    <select id="getAllInfo" resultType="com.chs.domain.Update">
        select meme.Id as meme_Id,user_Id,file,upload_time,nickname,profile_picture
        from meme,user,following
        where user_Id=user.Id and follower=#{follower} and user_Id=followee
        order by upload_time desc;
    </select>
    <select id="getInfoById" resultType="com.chs.domain.Update">
        select meme.Id as meme_Id,user_Id,file,upload_time,nickname,profile_picture
        from meme,user where user_Id=user.Id and user_Id=#{user_Id}
        order by upload_time desc
    </select>
</mapper>