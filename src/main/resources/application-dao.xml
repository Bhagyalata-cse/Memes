<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- 解析配置文件 -->
    <!-- system-properties-mode 如果不配置，读取的就不是数据库的用户名了，将会读取电脑本机的用户名 -->
    <context:property-placeholder location="classpath:jdbc.properties" system-properties-mode="FALLBACK"/>

    <!-- 事务管理器 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- 启用声明式事务 -->
    <tx:annotation-driven/>

    <!-- 声明数据源 -->
    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}"></property>
        <property name="url" value="${jdbc.url}"></property>
        <property name="username" value="${jdbc.username}"></property>
        <property name="password" value="${jdbc.password}"></property>
    </bean>

    <!-- 创建mybatis的 configuration对象-->
    <bean id="configuration" class="org.apache.ibatis.session.Configuration">
        <!--日志的输出形式 -->
        <property name="logImpl" value="org.apache.ibatis.logging.stdout.StdOutImpl"></property>
    </bean>

    <!-- 创建sqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!-- 注入数据源 -->
        <property name="dataSource" ref="dataSource"></property>
        <!-- 注入配置类 -->
        <property name="configuration" ref="configuration"></property>

        <!-- 扫描mapper.xml -->
        <property name="mapperLocations">
            <array>
                <value>classpath:com/chs/dao/*Mapper.xml</value>
            </array>
        </property>
        <!-- 别名包扫描 -->
        <property name="typeAliasesPackage" value="com.chs.domain" />
    </bean>

    <!-- 配置mapper接口的扫描 -->
    <!-- 因为mapper接口和mapper.xml文件分开放了所以这里扫描接口，上面是扫描xml文件  -->
    <bean id="mapperScannerConfigurer" class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!-- 如果要扫描多个包。用逗号隔开（第一种方法） -->
        <property name="basePackage" value="com.chs.dao"></property>

        <!-- 注入sqlSessionFactory -->
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"></property>
    </bean>
</beans>